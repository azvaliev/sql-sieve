[project]
name = "training"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "modal>=1.2.6",
]

[dependency-groups]
dev = [
    "datasets>=4.3.0",
    "peft>=0.18.1",
    "torch>=2.8.0",
    "transformers>=4.57.6",
    "trl>=0.29.0",
    "wandb>=0.25.0",
]
